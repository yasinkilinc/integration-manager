package tr.com.atez.integration.manager.service;
/*
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import tr.com.atez.customer.notify.manager.entity.CustomerEntity;
import tr.com.atez.customer.notify.manager.entity.CustomerEventDefinitionEntity;
import tr.com.atez.customer.notify.manager.entity.DataTypeDefinitionEntity;
import tr.com.atez.customer.notify.manager.entity.EventDefinitionEntity;

@SpringBootTest
class CustomerEventDefinitionServiceImplTest {

    @Autowired
    private CustomerEventDefinitionService customerEventDefinitionService;

    @Test
    public void TestCustomerEventDefinitionSave(){
        CustomerEntity customerEntity = new CustomerEntity();
        EventDefinitionEntity eventDefinitionEntity = new EventDefinitionEntity();
        DataTypeDefinitionEntity dataTypeDefinitionEntity = new DataTypeDefinitionEntity();

        CustomerEventDefinitionEntity customerEventDefinitionEntity = new CustomerEventDefinitionEntity();
        customerEventDefinitionEntity.setCustomer(customerEntity);
        customerEventDefinitionEntity.setEventDefinition(eventDefinitionEntity);
        customerEventDefinitionEntity.setDataTypeDefinition(dataTypeDefinitionEntity);

        customerEventDefinitionService.save(customerEventDefinitionEntity);
    }

}
 */