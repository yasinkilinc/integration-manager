package tr.com.atez.integration.manager.service;
/*
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import tr.com.atez.customer.notify.manager.entity.EventDefinitionEntity;

@SpringBootTest
class EventDefinitionEntityServiceImplTest {

    @Autowired
    private EventDefinitionService eventDefinitionService;

    @Test
    public void eventDefinitionSave(){
        EventDefinitionEntity parent = new EventDefinitionEntity();
        parent.setDescription("parent");
        EventDefinitionEntity child = new EventDefinitionEntity();
        child.setDescription("child");
        EventDefinitionEntity orphan = new EventDefinitionEntity();
        orphan.setDescription("orphan");

        parent.addChild(child);
        child.addChild(orphan);

        eventDefinitionService.save(parent);
    }



}

 */